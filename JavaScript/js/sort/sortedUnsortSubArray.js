let sortedUnsortSubArray = function (arr) {
    let left = 0;
    let right = arr.length - 1;
    for(let i =0; i< arr.length -1 ; i++) {
        if(a[i] >)
    }
}

console.log(sortedUnsortSubArray([1, 2, 4, 3, 5, 6, 7]));