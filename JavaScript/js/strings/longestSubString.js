/**
 * Given a String, find the longest substring with unique characters.
 * For example: "whatwhywhere" --> "atwhy"
 */

function allUnique(input) {
    let start = 0;
    let end = 0;
    let set = [input.charAt(0)];
    while(end < input.length - 1) {
        end++;
       
    }
    console.log(subSet);
}

console.log(allUnique("whatwhywhere"));